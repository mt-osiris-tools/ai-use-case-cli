# CLAUDE.md (Docs Mirror)

The canonical agent/workflow rules for this repository live at the repo root:

- `CLAUDE.md`

This file exists so tooling that expects `docs/CLAUDE.md` (e.g., version/reference validators) can locate a stable docs path.
