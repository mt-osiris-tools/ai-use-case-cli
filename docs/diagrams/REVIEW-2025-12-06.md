# Diagram Review - 2025-12-06

## Summary

The diagrams in `docs/diagrams/` need **moderate updates** to reflect recent architectural changes, particularly the **Agent Framework** (Phase 1 & 2) and **AI Tool Agnostic refactoring**.

## Changes Requiring Diagram Updates

### ✅ Major Changes - HIGH PRIORITY

#### 1. **Agent Framework Addition** (v3.11.0 - Phases 1 & 2)

**Impact:** NEW architectural component added

**What Changed:**
- New directory: `scripts/agents/`
- New scripts: `agent-registry.sh`, `invoke-agent.sh`, `quality-agent.sh`
- New configuration: `~/.config/ai-use-case-cli/agents.json`
- New CLI commands: `ai-use-case agents`, `ai-use-case review-quality`
- New slash command: `/use-case:review-quality`
- 4 registered agents: quality-reviewer, pattern-analyzer, session-selector, organization-optimizer

**Diagrams Affected:**
- **C4 Container Diagram** - Missing "Agent Framework" container
- **C4 Deployment Diagram** - Missing `agents.json` in configuration files
- **C4 Component Diagram** - Could add separate diagram for Agent Framework components

**Recommended Updates:**

For **Container Diagram** (c4-architecture.puml lines 31-101):
```plantuml
Container(agent_framework, "Agent Framework", "Bash", "Agent lifecycle management, invocation, caching, and statistics tracking")
ContainerDb(config_db, "Configuration", "JSON", "Hub mode (local/git), tracing config, project registry, agent registry")

Rel(cli_dispatcher, agent_framework, "Routes agent commands to", "Function call")
Rel(agent_framework, config_db, "Reads/writes agent registry", "JSON")
Rel(core_scripts, agent_framework, "Invokes quality review", "Shell execution")
```

For **Deployment Diagram** (c4-architecture.puml lines 144-207):
```plantuml
Deployment_Node(user_config, "User Configuration", "~/.config/ai-use-case-cli") {
    ContainerDb(config_files, "Configuration Files", "JSON", "config.json, tracing.json, agents.json")
}
```

#### 2. **AI Tool Agnostic Refactoring** (v3.11.0)

**Impact:** Terminology and naming changes

**What Changed:**
- Directory: `.claude/` → `.ai-tools/`
- Support for multiple AI tools: Claude Code, GitHub Copilot, OpenAI Codex
- Slash commands now in `.ai-tools/commands/use-case/`
- Templates changed from "Claude Code:" to "AI-Assisted:"

**Diagrams Affected:**
- **C4 Context Diagram** - References "Claude Code" specifically
- **C4 Container Diagram** - "Claude Code Integration" container name
- **C4 Deployment Diagram** - Project workspace doesn't show `.ai-tools/`

**Recommended Updates:**

For **Context Diagram** (c4-architecture.puml lines 1-29):
- Line 12: Keep "Claude Code" as one example, but generalize description
```plantuml
System_Ext(claude_code, "AI Coding Assistants", "AI-powered IDE extensions (Claude Code, GitHub Copilot, etc.) that execute slash commands")
```

For **Container Diagram** (c4-architecture.puml line 52):
```plantuml
Container(ai_integration, "AI Assistant Integration", "Markdown", "Slash command definitions for AI-assisted automation (Claude Code, Copilot, etc.)")
```

For **Deployment Diagram** - Update project workspace to show `.ai-tools/`:
```plantuml
Deployment_Node(project_workspace, "Project Directory", "/path/to/project") {
    Container(project_repo, "Project Repository", "Git", "Contains .usecase/cases/ and .ai-tools/ with slash commands")
    Container(git_hooks_deployed, "Git Hooks", "Bash", "Pre-commit and post-commit hooks")
}
```

### ✅ Minor Changes - MEDIUM PRIORITY

#### 3. **Templates Location** (Already Correct)
- Templates moved to CLI's `docs/` directory (not hub)
- Diagrams correctly show templates accessed from CLI, not hub
- **No changes needed**

#### 4. **New Slash Commands** (v3.11.0)
- Added: `/use-case:review-quality`
- Total slash commands: 11 (was 10)
- **Diagrams don't show individual commands, so no update needed**

### ℹ️ No Changes Needed - LOW PRIORITY

#### 5. **Document Session Sequence Diagram**
- Workflow remains accurate for v3.4.0+ features
- Could optionally add quality review step, but not required
- **Keep as-is for now**

## Recommended Action Plan

### Phase 1: Essential Updates (30 minutes)

1. **Update C4 Container Diagram** (c4-architecture.puml lines 31-101)
   - Add "Agent Framework" container
   - Rename "Claude Code Integration" to "AI Assistant Integration"
   - Add agents.json to Configuration database
   - Add relationships showing CLI → Agent Framework

2. **Update C4 Context Diagram** (c4-architecture.puml lines 1-29)
   - Generalize "Claude Code" to "AI Coding Assistants" with examples
   - Keep backward compatibility by mentioning Claude Code

3. **Update C4 Deployment Diagram** (c4-architecture.puml lines 144-207)
   - Add agents.json to configuration files
   - Update project directory to show `.ai-tools/`

### Phase 2: Optional Enhancements (60 minutes)

4. **Create Agent Framework Component Diagram**
   - New diagram showing Agent Framework internals
   - Components: agent-registry.sh, invoke-agent.sh, quality-agent.sh
   - Show interactions with config, CLI, and scripts

5. **Regenerate SVG Files**
   - After PlantUML source updates, regenerate all SVGs
   ```bash
   plantuml -tsvg docs/diagrams/c4-architecture.puml
   # or
   docker run --rm -v $(pwd):/data plantuml/plantuml:latest -tsvg docs/diagrams/c4-architecture.puml
   ```

## Files to Update

1. `docs/diagrams/c4-architecture.puml` - Main source file
2. `docs/diagrams/AI Use Case CLI - C4 Context Diagram.svg` - Regenerate
3. `docs/diagrams/AI Use Case CLI - C4 Container Diagram.svg` - Regenerate
4. `docs/diagrams/AI Use Case CLI - C4 Deployment Diagram.svg` - Regenerate
5. `docs/diagrams/README.md` - Update description if adding new diagrams

## Verification Checklist

After updates, verify:
- [ ] All container names match current terminology (AI Assistant, not Claude Code)
- [ ] Agent Framework container included with proper relationships
- [ ] agents.json shown in configuration
- [ ] `.ai-tools/` directory shown in deployment
- [ ] SVG files regenerated from updated PlantUML source
- [ ] README.md updated with any new diagram descriptions
- [ ] All diagram viewing options still work (online, VS Code, CLI, Docker)

## Notes

- The diagrams were created during v3.11.0 development, so they're relatively recent
- Agent Framework is the biggest architectural change since diagram creation
- AI tool agnosticism is mainly a terminology/branding change
- Core architecture remains solid - these are incremental updates
- Backward compatibility maintained throughout

---

**Review Date:** 2025-12-06
**Reviewed By:** AI Assistant
**CLI Version:** v3.11.0
**Recommendation:** Update diagrams in next maintenance cycle or before v3.12.0 release
